<div id="script__comment__0" class="layout__comments__new">

  <div v-cloak>
    <%= form_for(new_comment, url: create_comment_path) do |f| %>
        <%= render 'comments/form', f: f, index: 0, tale: tale %>
    <% end %>
  </div>

  <a v-on:click="showModal" class="module__button module__button--small module__button--wide"><%= t('.button') %></a>
</div>
